Setup:
1. download python, uv,
2. Gather data —> uv run download.py 
- Setup Supabase: 
    - anon -> pushlisable key 

3. Create todos.md and ai todos of what to do in app
- phases and checklists 

4. Phase 1: backend scalfold & database 
- [uv sync] gives virtual environment
- uv add pytest ruff 
- uv add --dev ipykernel
- main.py: create health endpoint and backend endpoint (uv run uvicorn app.main:app --reload to run app)
- create our database models 
- setup migrations, run m (uv run alembic upgrade head), check status of m (uv run alembic current  uv run alembic history)
- backend scalfold and setup enviornment variables 

5. Phase 2: frontend 
- pnpm create vite . --template react-ts
- pnpm add react-router-dom @supabase/supabase-js
- pnpm add -D tailwindcss @tailwindcss/vite (setup tailwind imports with AI-help)
- pnpm dlx shadcn@latest init
- Setup frontend scalfold and setup enviornment variables 

6. Authenication 
- Supabsae check email options and setup url confirguration 
- setup signup pages 

7. Implement backend and frontend 
- Componets for frontend 
- API-design for backend 

8. Ingestion Chunking
- uv add --dev docline (for parsing html files)
- create scripts and use docling to convert html to markdowns
- review documents and edit imperfections using docklane 
- create vector embedding and input the document markdown chunks with docling chunker
- run the ingestion, embeddings, or database uploads

9. Retrival Pipeline = user query -> semantic search (hybrid search + agentic search)
- implement hybrid search for agentic rag with ai-cookbook
- Ask if key-word search help
- Problem retrival pipeline and grounding validator 
    - model’s citation IDs were unreliable
    - the filing tables were poorly flattened during ingestion
    - relevant data crossed chunk boundaries overlapped.


10. FTS pipeline = extra meaningful keywords from prompt using nlk library for search words
- use shadcn + research best user chat expierence practices 
- use claude to preform gap analyze and fix design
- fixed the blockers, bugs, filling in the gaps for the pipelines